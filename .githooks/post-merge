#!/usr/bin/env sh
# Re-sync the clone's explicitly selected skill discovery roots after a pull.
# Installed by scripts/install.sh. Never fails the git operation.
"$(git rev-parse --show-toplevel)/scripts/install.sh" --quiet --from-hook || true
